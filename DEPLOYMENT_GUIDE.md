# 🚀 Гайд по развертыванию Alpha Parser на сервере

Полное руководство по установке и настройке Alpha Parser с использованием Docker Compose на продакшн сервере.

## 📋 Содержание

1. [Требования](#требования)
2. [Подготовка сервера](#подготовка-сервера)
3. [Установка Docker и Docker Compose](#установка-docker-и-docker-compose)
4. [Настройка проекта](#настройка-проекта)
5. [Создание файла конфигурации](#создание-файла-конфигурации)
6. [Запуск проекта](#запуск-проекта)
7. [Доступ к файлам (логи и БД)](#доступ-к-файлам-логи-и-бд)
8. [Управление контейнерами](#управление-контейнерами)
9. [Обновление проекта](#обновление-проекта)
10. [Устранение проблем](#устранение-проблем)

---

## 🔧 Требования

- Сервер с Linux (Ubuntu 20.04+ / Debian 11+ рекомендуется)
- Минимум 2 GB RAM
- Минимум 10 GB свободного места на диске
- Доступ по SSH
- Права root или sudo

---

## 🖥️ Подготовка сервера

### 1. Подключитесь к серверу

```bash
ssh user@your-server-ip
```

### 2. Обновите систему

```bash
sudo apt update && sudo apt upgrade -y
```

### 3. Создайте директорию для проекта

```bash
sudo mkdir -p /opt/alpha-parser/data
sudo chown -R $USER:$USER /opt/alpha-parser
```

---

## 🐳 Установка Docker и Docker Compose

### Установка Docker

```bash
# Удалите старые версии (если есть)
sudo apt remove docker docker-engine docker.io containerd runc

# Установите зависимости
sudo apt install -y \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# Добавьте официальный GPG ключ Docker
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Настройте репозиторий
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установите Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Добавьте пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER

# Перезайдите в систему или выполните:
newgrp docker

# Проверьте установку
docker --version
docker compose version
```

---

## 📦 Настройка проекта

### 1. Клонируйте репозиторий

```bash
cd /opt/alpha-parser
git clone https://github.com/alezzinap1/alpha_parser_pub.git .
```

Или скопируйте файлы проекта на сервер:

```bash
# С локального компьютера
scp -r /path/to/alpha-parser/* user@server:/opt/alpha-parser/
```

### 2. Убедитесь, что файлы на месте

```bash
cd /opt/alpha-parser
ls -la
# Должны быть: Dockerfile, docker-compose.yml, entrypoint.sh, src/, requirements.txt
```

---

## ⚙️ Создание файла конфигурации

### 1. Создайте файл `.env` в `/opt/alpha-parser/data/.env`

```bash
nano /opt/alpha-parser/data/.env
```

### 2. Заполните все необходимые переменные:

```env
# Режим работы
ENV_MODE=production

# Telegram API
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
TELEGRAM_PHONE_NUMBER=+1234567890
TELEGRAM_PASSWORD=your_2fa_password  # Оставьте пустым, если нет 2FA

# DeepSeek API
DEEPSEEK_API_KEY=your_deepseek_key

# Прокси (опционально)
OPENAI_PROXY=

# Google Sheets URL
CSV_URL=https://docs.google.com/spreadsheets/d/...

# DEFAULT_CONFIG_JSON - JSON строка с настройками
# ВАЖНО: Экранируйте кавычки и переносы строк!
DEFAULT_CONFIG_JSON={"target_channel":"@your_channel","system_prompt":"...","user_prompt":"...","max_messages_per_channel":100,"table_scan_interval":7200}

# Опционально: переопределение путей
# SESSION_NAME=userbot2_session
# DB_FILE=channels_v2.db
# LOG_FILE=userbot2.log
```

### 3. Сохраните файл (Ctrl+O, Enter, Ctrl+X в nano)

### 4. Установите правильные права доступа

```bash
chmod 600 /opt/alpha-parser/data/.env
```

⚠️ **ВАЖНО:** Файл `.env` содержит секретные данные! Никогда не коммитьте его в git!

---

## 🚀 Запуск проекта

### 1. Перейдите в директорию проекта

```bash
cd /opt/alpha-parser
```

### 2. Соберите Docker образ

```bash
docker compose build
```

### 3. Запустите контейнеры

```bash
docker compose up -d
```

### 4. Проверьте статус

```bash
docker compose ps
```

Вы должны увидеть:
```
NAME                    STATUS
alpha-parser           Up
alpha-parser-files     Up
```

### 5. Проверьте логи

```bash
# Логи основного бота
docker compose logs -f alpha-parser

# Логи файл-сервера
docker compose logs -f file-server

# Все логи
docker compose logs -f
```

---

## 📥 Доступ к файлам (логи и БД)

### Вариант 1: Через HTTP файл-сервер (рекомендуется)

Файл-сервер автоматически запускается на порту **8080** и предоставляет доступ ко всем файлам в `/opt/alpha-parser/data`.

#### Скачивание через браузер:

1. Откройте в браузере: `http://your-server-ip:8080/`
2. Вы увидите список всех файлов:
   - `channels_v2.db` - база данных
   - `userbot2.log` - логи
   - `userbot2_session.session` - сессия Telegram
   - `.env` - конфигурация (⚠️ не скачивайте через браузер!)

3. Кликните на нужный файл для скачивания

#### Скачивание через curl/wget:

```bash
# Скачать БД
curl http://your-server-ip:8080/channels_v2.db -o channels_v2_backup.db

# Скачать логи
curl http://your-server-ip:8080/userbot2.log -o userbot2_backup.log

# Или через wget
wget http://your-server-ip:8080/channels_v2.db
```

#### Скачивание с локального компьютера:

```bash
# Windows PowerShell
Invoke-WebRequest -Uri "http://your-server-ip:8080/channels_v2.db" -OutFile "channels_v2_backup.db"

# Linux/Mac
curl http://your-server-ip:8080/channels_v2.db -o channels_v2_backup.db
```

### Вариант 2: Прямой доступ через SSH

```bash
# На сервере
cd /opt/alpha-parser/data
ls -la

# Скачать через SCP (с локального компьютера)
scp user@server:/opt/alpha-parser/data/channels_v2.db ./channels_v2_backup.db
scp user@server:/opt/alpha-parser/data/userbot2.log ./userbot2_backup.log
```

### Вариант 3: Через Docker exec

```bash
# Войти в контейнер
docker exec -it alpha-parser bash

# Файлы находятся в /data
cd /data
ls -la
sqlite3 channels_v2.db
```

---

## 🎮 Управление контейнерами

### Просмотр статуса

```bash
docker compose ps
```

### Просмотр логов

```bash
# Все логи
docker compose logs

# Логи с обновлением в реальном времени
docker compose logs -f

# Логи конкретного сервиса
docker compose logs -f alpha-parser
docker compose logs -f file-server

# Последние 100 строк
docker compose logs --tail=100 alpha-parser
```

### Остановка

```bash
docker compose stop
```

### Запуск

```bash
docker compose start
```

### Перезапуск

```bash
docker compose restart

# Перезапуск конкретного сервиса
docker compose restart alpha-parser
```

### Остановка и удаление контейнеров

```bash
docker compose down
```

⚠️ **ВНИМАНИЕ:** Это остановит контейнеры, но **НЕ удалит** данные в `/opt/alpha-parser/data`!

### Полная очистка (удаление контейнеров и образов)

```bash
docker compose down --rmi all
```

⚠️ **ВНИМАНИЕ:** Это удалит Docker образы, но **НЕ удалит** данные в `/opt/alpha-parser/data`!

---

## 🔄 Обновление проекта

### 1. Остановите контейнеры

```bash
cd /opt/alpha-parser
docker compose down
```

### 2. Обновите код

```bash
# Если используете git
git pull

# Или скопируйте новые файлы
# scp -r /path/to/alpha-parser/* user@server:/opt/alpha-parser/
```

### 3. Пересоберите образ

```bash
docker compose build --no-cache
```

### 4. Запустите заново

```bash
docker compose up -d
```

### 5. Проверьте логи

```bash
docker compose logs -f alpha-parser
```

---

## 🔍 Устранение проблем

### Проблема: Контейнер не запускается

**Решение:**
```bash
# Проверьте логи
docker compose logs alpha-parser

# Проверьте статус
docker compose ps

# Проверьте файл .env
cat /opt/alpha-parser/data/.env

# Проверьте права доступа
ls -la /opt/alpha-parser/data
```

### Проблема: "Environment variable X must be set"

**Решение:**
- Проверьте, что файл `.env` существует: `ls -la /opt/alpha-parser/data/.env`
- Проверьте содержимое: `cat /opt/alpha-parser/data/.env`
- Убедитесь, что все переменные заполнены
- Проверьте, что `DEFAULT_CONFIG_JSON` - валидный JSON

### Проблема: Файл-сервер недоступен

**Решение:**
```bash
# Проверьте, запущен ли контейнер
docker compose ps file-server

# Проверьте логи
docker compose logs file-server

# Проверьте, открыт ли порт 8080
sudo netstat -tlnp | grep 8080

# Перезапустите файл-сервер
docker compose restart file-server
```

### Проблема: Не могу скачать файлы через браузер

**Решение:**
- Проверьте, что порт 8080 открыт в firewall:
  ```bash
  sudo ufw allow 8080/tcp
  ```
- Проверьте, что файл-сервер запущен: `docker compose ps`
- Попробуйте через curl: `curl http://localhost:8080/`

### Проблема: База данных не создается

**Решение:**
- Проверьте права доступа к `/opt/alpha-parser/data`:
  ```bash
  sudo chown -R $USER:$USER /opt/alpha-parser/data
  sudo chmod -R 755 /opt/alpha-parser/data
  ```
- Проверьте логи: `docker compose logs alpha-parser`

### Проблема: Бот не подключается к Telegram

**Решение:**
- Проверьте правильность `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
- Проверьте правильность `TELEGRAM_PHONE_NUMBER`
- Проверьте логи: `docker compose logs -f alpha-parser`
- Убедитесь, что сессия создана: `ls -la /opt/alpha-parser/data/*.session`

### Проблема: Контейнер постоянно перезапускается

**Решение:**
```bash
# Проверьте логи
docker compose logs --tail=100 alpha-parser

# Проверьте статус
docker compose ps

# Попробуйте запустить вручную для отладки
docker compose run --rm alpha-parser
```

---

## 🔒 Безопасность

### Рекомендации:

1. **Ограничьте доступ к файл-серверу:**
   - Используйте firewall для ограничения доступа к порту 8080 только с вашего IP
   - Или используйте VPN для доступа к серверу

2. **Защитите файл `.env`:**
   ```bash
   chmod 600 /opt/alpha-parser/data/.env
   ```

3. **Регулярно делайте бэкапы:**
   ```bash
   # Создайте скрипт бэкапа
   #!/bin/bash
   DATE=$(date +%Y%m%d_%H%M%S)
   tar -czf /opt/alpha-parser/backups/backup_$DATE.tar.gz -C /opt/alpha-parser/data channels_v2.db userbot2.log userbot2_session.session
   ```

4. **Настройте автоматические бэкапы через cron:**
   ```bash
   crontab -e
   # Добавьте строку:
   0 2 * * * /path/to/backup_script.sh
   ```

---

## 📊 Мониторинг

### Проверка использования ресурсов

```bash
# Использование CPU и памяти
docker stats

# Использование диска
df -h
du -sh /opt/alpha-parser/data
```

### Проверка размера БД

```bash
# На сервере
ls -lh /opt/alpha-parser/data/channels_v2.db

# Или через SQLite
sqlite3 /opt/alpha-parser/data/channels_v2.db "SELECT COUNT(*) FROM posts;"
```

---

## 📝 Полезные команды

```bash
# Просмотр всех контейнеров
docker ps -a

# Просмотр всех образов
docker images

# Очистка неиспользуемых ресурсов
docker system prune -a

# Просмотр использования диска Docker
docker system df

# Вход в контейнер
docker exec -it alpha-parser bash

# Выполнение команды в контейнере
docker exec alpha-parser python -m src.RUN4 --help
```

---

## 🆘 Получение помощи

Если возникли проблемы:

1. Проверьте логи: `docker compose logs -f`
2. Проверьте статус: `docker compose ps`
3. Проверьте конфигурацию: `cat /opt/alpha-parser/data/.env`
4. Обратитесь к документации в `README.md`

---

## ✅ Чек-лист после установки

- [ ] Docker и Docker Compose установлены
- [ ] Проект склонирован/скопирован на сервер
- [ ] Файл `.env` создан и заполнен
- [ ] Контейнеры запущены (`docker compose ps`)
- [ ] Логи показывают успешный запуск
- [ ] Файл-сервер доступен на порту 8080
- [ ] Можно скачать файлы через браузер/curl
- [ ] Бот успешно подключился к Telegram
- [ ] База данных создается и заполняется

---

**Готово!** 🎉 Ваш Alpha Parser развернут и готов к работе!

